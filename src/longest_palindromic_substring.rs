struct Solution {}

impl Solution {
    pub fn longest_palindrome(s: String) -> String {
        "".to_owned()
    }
}

#[test]
fn test_longest_palindromic_substringv() {
    assert_eq!("cbbd".to_owned(), "bb".to_owned());
}
